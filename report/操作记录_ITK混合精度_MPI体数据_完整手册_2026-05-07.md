# ITK 混合精度（单机 + MPI + 真实体数据）— 完整手册

**日期：2026-05-07**  
**本地归档：** `D:\ECNU_HPC\ITK_huawei\2026-0507`  

本文档将 **混合精度的设计说明、代码修改履历、完整源代码、运行环境、编译/运行命令与实测终端日志** 集中归档，便于复查与交接。  
精简版备份说明仍见 [`操作记录_ITK备份与混合精度_2026-05-07.md`](./操作记录_ITK备份与混合精度_2026-05-07.md)；MPI 通用操作仍见 [`操作记录_MPI双机环境与运行_2026-05-07.md`](./操作记录_MPI双机环境与运行_2026-05-07.md)。

---

## 目录

1. [运行环境与路径](#1-运行环境与路径)  
2. [混合精度含义与实现要点](#2-混合精度含义与实现要点)  
3. [代码修改履历（按问题驱动）](#3-代码修改履历按问题驱动)  
4. [ITK 源码备份（摘要）](#4-itk-源码备份摘要)  
5. [工程一：`itk_hybrid_precision_demo`（单机合成图）](#5-工程一itk_hybrid_precision_demo单机合成图)  
6. [工程二：`mpi_hybrid_precision_demo`（MPI + Reader + 体数据）](#6-工程二mpi_hybrid_precision_demompi--reader--体数据)  
7. [封装脚本 `run_mpi_hybrid_two_nodes.sh`](#7-封装脚本-run_mpi_hybrid_two_nodessh)  
8. [实测日志归档](#8-实测日志归档)  
9. [相关索引](#9-相关索引)

---

## 1. 运行环境与路径

下列值为 **2026-05-07 会话中实际使用** 的配置；若集群升级请以机器为准。

| 类别 | 项目 | 说明 |
|------|------|------|
| 节点 | 第一台 | `202.120.87.20`，主机名 **`hpc01`**，`nproc=96` |
| 节点 | 第二台 | `202.120.87.128`，主机名 **`hpc03`**，`nproc=96` |
| 架构 | CPU | **aarch64**（鲲鹏类） |
| 编译器 | GCC | **`/home/pub/gjj/gcc10/bin/g++`**，**10.1.0** |
| 构建 | CMake | **`/home/pub/cmake/bin/cmake`**，**3.27.1**（勿用系统 `/usr/local/bin/cmake` 3.6.x 配置 ITK 5.4） |
| MPI | Open MPI | **`/usr/local/bin/mpirun`**，**4.0.1**；**`mpicxx`** 建议配合 **`OMPI_CXX=/home/pub/gjj/gcc10/bin/g++`** |
| ITK | 源码 | **`/home/pub/yyq/ITK-5.4.0`** |
| ITK | 构建树 | **`/home/pub/yyq/ITK-5.4.0/build-yyq`**（`-DITK_DIR=...` 指向此处） |
| 单机混合精度工程 | 远端路径 | **`/home/pub/yyq/itk_hybrid_precision_demo/`** |
| MPI 混合精度工程 | 远端路径 | **`/home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo`** |
| 双机 hostfile | 远端路径 | **`/home/pub/yyq_archive/mpi_hosts_2nodes.txt`** |
| 示例体数据 | 路径（两台均有源码树时） | **`/home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha`** |

**环境与 ITK 全局编译注意：** 若在登录 shell 中加载 **`kunpeng-simd.sh`**，可能注入 **`-march=native`**，与旧 **binutils** 组合会导致 GCC 探测失败；配置 **ITK / 本手册两类工程** 时常用 **`env -i HOME=... PATH=/home/pub/gjj/gcc10/bin:/home/pub/cmake/bin:/usr/bin:/usr/local/bin ...`**（详见 [`操作记录_ITK编译与单节点并行绑核_2026-05-07.md`](./操作记录_ITK编译与单节点并行绑核_2026-05-07.md)）。

---

## 2. 混合精度含义与实现要点

- ITK **没有**深度学习框架式的全局「自动 FP16/BF16 混合精度」。  
- 典型做法：用 **`itk::CastImageFilter`** 在 **`float`** 与 **`double`** 图像类型之间切换；关键滤波器用 **`Image<double, Dim>`** 实例化，其余保持 **`float`** 以降低内存与带宽。  
- 本仓库两套示例均采用：**`Cast float→double` → `itk::SmoothingRecursiveGaussianImageFilter<double,...>` → `Cast→float`** 作为「混合支路」，并与 **全程 `float` 同名滤波器** 做逐像素统计对比：  
  - **`[D vs F]`**：double 高斯 vs float 高斯；  
  - **`[D vs H]`**：double 高斯 vs 混合支路最终 **float**（观察 **压回 float** 的量化）。  
- **`itk::SmoothingRecursiveGaussianImageFilter`** 要求 **每一维尺寸 ≥ 4**；薄层体数据（如 Z=3）必须先 **pad** 或调整切分策略（见第 3 节）。

---

## 3. 代码修改履历（按问题驱动）

| 次序 | 现象 / 需求 | 处理 |
|------|-------------|------|
| 1 | 单机示例需可拷贝到 **libstdc++ 较旧** 的节点 | 两个工程的 **`CMakeLists.txt`** 对 **GNU** 增加 **`-static-libgcc -static-libstdc++`**。 |
| 2 | 仅用末端均为 **float** 对比时，数值差常为 **0** | **`itk_hybrid_precision_demo`** 改为显式比较 **double 平滑图 vs float 平滑图**，并比较 **double vs hybrid 最终 float**（见源码 §5）。 |
| 3 | **`BrainProtonDensity3Slices.mha`** 沿 **Z** 只有 **3** 层，`SmoothingRecursiveGaussian` 报错 | **`mpi_hybrid_precision_demo`**：读入后对 **整卷** 做 **`ConstantPadImageFilter`**，任一侧 **尺寸 &lt; 4** 则 **pad 到 4**。 |
| 4 | **全局 pad 后 Z=4**，仍按 **Z** 轴切 **8** 进程 → 部分 rank 的 ROI **Z=1**，再次触发 **dimension 2 &lt; 4** | 改为沿 **当前体积最长的一维**（通常为 **X/Y**）划分 **`ExtractImageFilter`** 区域，而非固定最后一维。 |
| 5 | 极端切分下 ROI 仍可能某维 **&lt; 4** | 对每个 **ROI** 再执行一次 **`ConstantPadImageFilter`**（子块 pad）。 |
| 6 | 双机运行时 **hpc03** 仍报旧错误 | **原因：** 第二台仍为 **旧二进制**。**处理：** **`scp`** 覆盖 **`/home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo`** 并 **`chmod +x`**。 |

---

## 4. ITK 源码备份（摘要）

| 归档文件 | 说明 |
|----------|------|
| **`/home/pub/yyq_archive/yyq_ITK542_src_2026-05-07.tar.gz`** | ITK-5.4.0 **源码**（排除 **`build`/`build-yyq`**）+ `run_itk_single_node_bind.sh`，约 **22 MB** |
| **MD5** | **`053f1657e99f4b21456ccf90927976fa`**（以服务器 **`md5sum`** 为准） |

重新打包时请在 SSH 外层使用 **单引号**，避免 Windows PowerShell 误解析 **`$(date +%F)`**：

```bash
ssh root@202.120.87.20 'D=$(date +%F); cd /home/pub/yyq && tar --exclude=ITK-5.4.0/build --exclude=ITK-5.4.0/build-yyq -czf /home/pub/yyq_archive/yyq_ITK542_src_${D}.tar.gz ITK-5.4.0 run_itk_single_node_bind.sh && ls -lh /home/pub/yyq_archive/yyq_ITK542_src_${D}.tar.gz && md5sum /home/pub/yyq_archive/yyq_ITK542_src_${D}.tar.gz'
```

---

## 5. 工程一：`itk_hybrid_precision_demo`（单机合成图）

### 5.1 作用

不依赖 MPI、不读盘：内存合成 **256×256 float** 棋盘图 → **Median** → **混合精度高斯** vs **float 高斯**，打印 **`[A]`/`[B]`** 误差指标。

### 5.2 `CMakeLists.txt`（全文）

```cmake
cmake_minimum_required(VERSION 3.16)
project(itk_hybrid_precision_demo LANGUAGES CXX)

find_package(ITK REQUIRED COMPONENTS ITKSmoothing ITKImageIntensity)
include(${ITK_USE_FILE})

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

add_executable(itk_hybrid_precision_demo hybrid_precision_demo.cxx)
target_link_libraries(itk_hybrid_precision_demo PRIVATE ${ITK_LIBRARIES})

if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
  target_link_options(itk_hybrid_precision_demo PRIVATE -static-libgcc -static-libstdc++)
endif()
```

### 5.3 `hybrid_precision_demo.cxx`（全文）

```cpp
// 演示 ITK 中的「混合精度」流水线：存储与大部分步骤用 float，关键平滑步骤用 double。
// ITK 无统一的自动混合精度运行时；典型做法是在滤波器链上用 itk::CastImageFilter 切换像素类型。

#include "itkCastImageFilter.h"
#include "itkImage.h"
#include "itkImageRegionIterator.h"
#include "itkMedianImageFilter.h"
#include "itkSmoothingRecursiveGaussianImageFilter.h"

#include <cmath>
#include <iomanip>
#include <iostream>

int
main(int, char **)
{
  constexpr unsigned int Dim = 2;
  using FloatImageType = itk::Image<float, Dim>;
  using DoubleImageType = itk::Image<double, Dim>;

  // --- 合成输入（float）：带台阶状图案，便于观察平滑与数值差异 ---
  auto input = FloatImageType::New();
  {
    FloatImageType::SizeType   size;
    FloatImageType::IndexType  index;
    FloatImageType::RegionType region;
    size.Fill(256);
    index.Fill(0);
    region.SetSize(size);
    region.SetIndex(index);
    input->SetRegions(region);
    input->Allocate();
    itk::ImageRegionIterator<FloatImageType> it(input, region);
    for (it.GoToBegin(); !it.IsAtEnd(); ++it)
    {
      const auto         idx = it.GetIndex();
      const unsigned int x = static_cast<unsigned int>(idx[0]);
      const unsigned int y = static_cast<unsigned int>(idx[1]);
      const float        v = ((x / 32) + (y / 32)) % 2 == 0 ? 0.0F : 1000.0F;
      it.Set(v);
    }
  }

  // --- float：中值去噪（低成本步骤）---
  using MedianF = itk::MedianImageFilter<FloatImageType, FloatImageType>;
  auto median = MedianF::New();
  {
    MedianF::InputSizeType r;
    r.Fill(1);
    median->SetRadius(r);
  }
  median->SetInput(input);

  // --- 升到 double：在高精度类型上做递归高斯平滑（对舍入更敏感的阶段）---
  using CastToDouble = itk::CastImageFilter<FloatImageType, DoubleImageType>;
  auto castUp = CastToDouble::New();
  castUp->SetInput(median->GetOutput());

  using SmoothD = itk::SmoothingRecursiveGaussianImageFilter<DoubleImageType, DoubleImageType>;
  auto smoothD = SmoothD::New();
  smoothD->SetInput(castUp->GetOutput());
  smoothD->SetSigma(2.0);

  // --- 降回 float：便于后续与 IO / 其它 float 模块对接 ---
  using CastToFloat = itk::CastImageFilter<DoubleImageType, FloatImageType>;
  auto castDown = CastToFloat::New();
  castDown->SetInput(smoothD->GetOutput());

  // --- 对照：整条链都在 float 上平滑（同一 sigma），用于对比混合精度结果 ---
  using SmoothF = itk::SmoothingRecursiveGaussianImageFilter<FloatImageType, FloatImageType>;
  auto smoothF = SmoothF::New();
  smoothF->SetInput(median->GetOutput());
  smoothF->SetSigma(2.0);

  smoothD->Update();
  smoothF->Update();
  castDown->Update();
  FloatImageType::Pointer hybridOut = castDown->GetOutput();
  DoubleImageType::Pointer doubleSmoothOut = smoothD->GetOutput();
  FloatImageType::Pointer  floatOnlyOut = smoothF->GetOutput();

  // --- 核心对比：同一平滑在 double 域 vs float 域的逐像素差异（再与「double 结果压回 float」对照）---
  double       sumAbsDF = 0.0;
  double       maxAbsDF = 0.0;
  double       sumAbsHF = 0.0;
  double       maxAbsHF = 0.0;
  unsigned long count = 0;
  itk::ImageRegionConstIterator<DoubleImageType> itD(doubleSmoothOut, doubleSmoothOut->GetBufferedRegion());
  itk::ImageRegionConstIterator<FloatImageType> itFf(floatOnlyOut, floatOnlyOut->GetBufferedRegion());
  itk::ImageRegionConstIterator<FloatImageType> itH(hybridOut, hybridOut->GetBufferedRegion());
  for (itD.GoToBegin(), itFf.GoToBegin(), itH.GoToBegin(); !itD.IsAtEnd(); ++itD, ++itFf, ++itH)
  {
    const double vd = itD.Get();
    const double vf = static_cast<double>(itFf.Get());
    const double adf = std::fabs(vd - vf);
    sumAbsDF += adf;
    if (adf > maxAbsDF)
    {
      maxAbsDF = adf;
    }
    const double vh = static_cast<double>(itH.Get());
    const double adh = std::fabs(vd - vh);
    sumAbsHF += adh;
    if (adh > maxAbsHF)
    {
      maxAbsHF = adh;
    }
    ++count;
  }
  const double maeDF = count > 0 ? sumAbsDF / static_cast<double>(count) : 0.0;
  const double maeHF = count > 0 ? sumAbsHF / static_cast<double>(count) : 0.0;

  std::cout << std::setprecision(12) << "hybrid_precision_demo: float median -> cast double -> Gaussian -> cast float\n";
  std::cout << "reference pipeline: float median -> float Gaussian (full float)\n";
  std::cout << "[A] double_Gaussian vs float_Gaussian: max_abs=" << maxAbsDF << " mean_abs=" << maeDF << '\n';
  std::cout << "[B] double_Gaussian vs hybrid_final_float: max_abs=" << maxAbsHF << " mean_abs=" << maeHF << '\n';
  std::cout << "pixels=" << count << '\n';

  return 0;
}
```

### 5.4 编译与运行命令（第一台）

```bash
cd /home/pub/yyq/itk_hybrid_precision_demo
rm -rf build && mkdir build && cd build

env -i HOME="$HOME" PATH=/home/pub/gjj/gcc10/bin:/home/pub/cmake/bin:/usr/bin:/usr/local/bin \
  cmake .. \
  -DCMAKE_CXX_COMPILER=/home/pub/gjj/gcc10/bin/g++ \
  -DITK_DIR=/home/pub/yyq/ITK-5.4.0/build-yyq

env -i HOME="$HOME" PATH=/home/pub/gjj/gcc10/bin:/home/pub/cmake/bin:/usr/bin:/usr/local/bin \
  cmake --build . -- -j8

./itk_hybrid_precision_demo
```

---

## 6. 工程二：`mpi_hybrid_precision_demo`（MPI + Reader + 体数据）

### 6.1 作用

- **`itk::ImageFileReader<float,3>`** 读取真实 **3D** 体数据；  
- **MPI**：按 rank 划分 **最长空间维** 上的子块；子块内执行 **混合精度高斯** 与 **float 高斯**；  
- **`MPI_Reduce`**：**全局 sum/max** 聚合 **`[D vs F]` / `[D vs H]`**。  

**演示级限制：** 每个 rank **整卷读入**；大体数据请改为并行 IO / 单 reader 广播元数据等方案。

### 6.2 `CMakeLists.txt`（全文）

```cmake
cmake_minimum_required(VERSION 3.16)
project(mpi_hybrid_precision_demo LANGUAGES CXX)

find_package(MPI REQUIRED COMPONENTS CXX)
find_package(
  ITK REQUIRED COMPONENTS ITKSmoothing ITKImageIntensity ITKImageGrid ITKIOImageBase ITKIOMeta ITKIOPNG
)
include(${ITK_USE_FILE})

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

add_executable(mpi_hybrid_precision_demo mpi_hybrid_precision_demo.cxx)
target_link_libraries(mpi_hybrid_precision_demo PRIVATE MPI::MPI_CXX ${ITK_LIBRARIES})

if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
  target_link_options(mpi_hybrid_precision_demo PRIVATE -static-libgcc -static-libstdc++)
endif()
```

### 6.3 `mpi_hybrid_precision_demo.cxx`（全文）

见仓库文件 [`mpi_hybrid_precision_demo/mpi_hybrid_precision_demo.cxx`](./mpi_hybrid_precision_demo/mpi_hybrid_precision_demo.cxx)（与以下快照一致，若仓库更新以文件为准）。

```cpp
// MPI + 真实体数据（ImageFileReader<float,3>）+ 混合精度（Cast float->double->Gaussian->float）。
// 沿「当前体积最长的一维」划分子块（薄层体积避免沿层厚方向切得过细），必要时对子块再 pad，
// 以满足 SmoothingRecursiveGaussian 各维 ≥4 像素的要求。
//
// 用法（在第一台发起多机时，各节点须能访问同一文件路径，参见文档）：
//   mpirun ... ./mpi_hybrid_precision_demo /path/to/volume.mha [sigma]
//
// 默认 sigma=1.5；可通过 argv[2] 覆盖。

#include <mpi.h>

#include "itkCastImageFilter.h"
#include "itkConstantPadImageFilter.h"
#include "itkExtractImageFilter.h"
#include "itkImage.h"
#include "itkImageFileReader.h"
#include "itkImageRegionConstIterator.h"
#include "itkSmoothingRecursiveGaussianImageFilter.h"

#include <cmath>
#include <cstdlib>
#include <iostream>
#include <string>

namespace
{

constexpr unsigned int Dimension = 3;
using FloatImageType = itk::Image<float, Dimension>;
using DoubleImageType = itk::Image<double, Dimension>;

double
ParseSigma(int argc, char ** argv)
{
  double sigma = 1.5;
  if (argc >= 3)
  {
    sigma = std::strtod(argv[2], nullptr);
    if (!(sigma > 0.0))
    {
      sigma = 1.5;
    }
  }
  return sigma;
}

} // namespace

int
main(int argc, char ** argv)
{
  MPI_Init(&argc, &argv);

  int rank = 0;
  int mpi_size = 1;
  MPI_Comm_rank(MPI_COMM_WORLD, &rank);
  MPI_Comm_size(MPI_COMM_WORLD, &mpi_size);

  if (argc < 2)
  {
    if (rank == 0)
    {
      std::cerr << "用法: " << (argc > 0 ? argv[0] : "mpi_hybrid_precision_demo")
                << " <体数据路径.mha|.mhd|.png...> [sigma]\n";
    }
    MPI_Finalize();
    return 1;
  }

  const std::string filename(argv[1]);
  const double      sigma = ParseSigma(argc, argv);

  try
  {
    using ReaderType = itk::ImageFileReader<FloatImageType>;
    auto reader = ReaderType::New();
    reader->SetFileName(filename);
    reader->Update();
    FloatImageType::Pointer volume = reader->GetOutput();

    // SmoothingRecursiveGaussianImageFilter 要求各维像素数 ≥ 4；薄体积（如仅 3 层切片）需先 pad。
    {
      auto                          sz = volume->GetLargestPossibleRegion().GetSize();
      FloatImageType::SizeType      lowerPad;
      FloatImageType::SizeType      upperPad;
      constexpr itk::SizeValueType kMinAxis = 4;
      lowerPad.Fill(0);
      upperPad.Fill(0);
      for (unsigned int d = 0; d < Dimension; ++d)
      {
        if (sz[d] < kMinAxis)
        {
          upperPad[d] = kMinAxis - sz[d];
        }
      }
      const bool needPad = [&]() {
        for (unsigned int d = 0; d < Dimension; ++d)
        {
          if (upperPad[d] != 0)
          {
            return true;
          }
        }
        return false;
      }();
      if (needPad)
      {
        using PadType = itk::ConstantPadImageFilter<FloatImageType, FloatImageType>;
        auto pad = PadType::New();
        pad->SetInput(volume);
        pad->SetPadLowerBound(lowerPad);
        pad->SetPadUpperBound(upperPad);
        pad->SetConstant(0.0f);
        pad->Update();
        volume = pad->GetOutput();
      }
    }

    const auto          inputRegion = volume->GetLargestPossibleRegion();
    auto                inputSize = inputRegion.GetSize();
    unsigned int        axis = 0;
    itk::SizeValueType  longest = inputSize[0];
    for (unsigned int d = 1; d < Dimension; ++d)
    {
      if (inputSize[d] > longest)
      {
        longest = inputSize[d];
        axis = d;
      }
    }
    const unsigned long extent = static_cast<unsigned long>(inputSize[axis]);

    const unsigned long z0 = (static_cast<unsigned long>(rank) * extent) / static_cast<unsigned long>(mpi_size);
    const unsigned long z1 =
      (static_cast<unsigned long>(rank + 1) * extent) / static_cast<unsigned long>(mpi_size);
    const unsigned long zlen = (z1 > z0) ? (z1 - z0) : 0UL;

    double       local_sum_abs_df = 0.0;
    double       local_max_abs_df = 0.0;
    double       local_sum_abs_hf = 0.0;
    double       local_max_abs_hf = 0.0;
    unsigned long local_pixels = 0UL;

    char proc[MPI_MAX_PROCESSOR_NAME];
    int  plen = 0;
    MPI_Get_processor_name(proc, &plen);

    if (zlen == 0UL)
    {
      std::cout << "rank " << rank << '/' << mpi_size << " on " << std::string(proc, static_cast<size_t>(plen))
                << " split_axis=" << axis << " range=[empty] pixels=0\n";
    }
    else
    {
      FloatImageType::RegionType subRegion = inputRegion;
      FloatImageType::IndexType  subIndex = subRegion.GetIndex();
      FloatImageType::SizeType   subSize = inputSize;
      subIndex[axis] += static_cast<itk::IndexValueType>(z0);
      subSize[axis] = static_cast<itk::SizeValueType>(zlen);
      subRegion.SetIndex(subIndex);
      subRegion.SetSize(subSize);

      using ExtractType = itk::ExtractImageFilter<FloatImageType, FloatImageType>;
      auto extract = ExtractType::New();
      extract->SetInput(volume);
      extract->SetExtractionRegion(subRegion);
      extract->Update();
      FloatImageType::Pointer roi = extract->GetOutput();

      // 子块某一维仍可能 <4（例如沿长轴切得很碎或原始层厚极小）；逐块 pad。
      {
        auto                     rsz = roi->GetLargestPossibleRegion().GetSize();
        FloatImageType::SizeType lowerPad;
        FloatImageType::SizeType upperPad;
        constexpr itk::SizeValueType kMinAxis = 4;
        lowerPad.Fill(0);
        upperPad.Fill(0);
        bool needRoiPad = false;
        for (unsigned int d = 0; d < Dimension; ++d)
        {
          if (rsz[d] < kMinAxis)
          {
            upperPad[d] = kMinAxis - rsz[d];
            needRoiPad = true;
          }
        }
        if (needRoiPad)
        {
          using PadRoi = itk::ConstantPadImageFilter<FloatImageType, FloatImageType>;
          auto proi = PadRoi::New();
          proi->SetInput(roi);
          proi->SetPadLowerBound(lowerPad);
          proi->SetPadUpperBound(upperPad);
          proi->SetConstant(0.0f);
          proi->Update();
          roi = proi->GetOutput();
        }
      }

      using CastToDouble = itk::CastImageFilter<FloatImageType, DoubleImageType>;
      auto castUp = CastToDouble::New();
      castUp->SetInput(roi);

      using SmoothD = itk::SmoothingRecursiveGaussianImageFilter<DoubleImageType, DoubleImageType>;
      auto smoothD = SmoothD::New();
      smoothD->SetInput(castUp->GetOutput());
      smoothD->SetSigma(sigma);

      using CastToFloat = itk::CastImageFilter<DoubleImageType, FloatImageType>;
      auto castDown = CastToFloat::New();
      castDown->SetInput(smoothD->GetOutput());

      using SmoothF = itk::SmoothingRecursiveGaussianImageFilter<FloatImageType, FloatImageType>;
      auto smoothF = SmoothF::New();
      smoothF->SetInput(roi);
      smoothF->SetSigma(sigma);

      smoothD->Update();
      smoothF->Update();
      castDown->Update();

      DoubleImageType::Pointer doubleSmoothOut = smoothD->GetOutput();
      FloatImageType::Pointer  floatOnlyOut = smoothF->GetOutput();
      FloatImageType::Pointer  hybridOut = castDown->GetOutput();

      itk::ImageRegionConstIterator<DoubleImageType> itD(doubleSmoothOut, doubleSmoothOut->GetBufferedRegion());
      itk::ImageRegionConstIterator<FloatImageType> itFf(floatOnlyOut, floatOnlyOut->GetBufferedRegion());
      itk::ImageRegionConstIterator<FloatImageType> itH(hybridOut, hybridOut->GetBufferedRegion());

      for (itD.GoToBegin(), itFf.GoToBegin(), itH.GoToBegin(); !itD.IsAtEnd(); ++itD, ++itFf, ++itH)
      {
        const double vd = itD.Get();
        const double vf = static_cast<double>(itFf.Get());
        const double vh = static_cast<double>(itH.Get());
        const double adf = std::fabs(vd - vf);
        const double adh = std::fabs(vd - vh);
        local_sum_abs_df += adf;
        local_sum_abs_hf += adh;
        if (adf > local_max_abs_df)
        {
          local_max_abs_df = adf;
        }
        if (adh > local_max_abs_hf)
        {
          local_max_abs_hf = adh;
        }
        ++local_pixels;
      }

      std::cout << "rank " << rank << '/' << mpi_size << " on " << std::string(proc, static_cast<size_t>(plen))
                << " split_axis=" << axis << " range=[" << z0 << ',' << z1 << ") pixels=" << local_pixels
                << " local_max|D-F|=" << local_max_abs_df << " local_max|D-H|=" << local_max_abs_hf << '\n';
    }

    double global_sum_abs_df = 0.0;
    MPI_Reduce(&local_sum_abs_df, &global_sum_abs_df, 1, MPI_DOUBLE, MPI_SUM, 0, MPI_COMM_WORLD);

    double global_max_abs_df = 0.0;
    MPI_Reduce(&local_max_abs_df, &global_max_abs_df, 1, MPI_DOUBLE, MPI_MAX, 0, MPI_COMM_WORLD);

    double global_sum_abs_hf = 0.0;
    MPI_Reduce(&local_sum_abs_hf, &global_sum_abs_hf, 1, MPI_DOUBLE, MPI_SUM, 0, MPI_COMM_WORLD);

    double global_max_abs_hf = 0.0;
    MPI_Reduce(&local_max_abs_hf, &global_max_abs_hf, 1, MPI_DOUBLE, MPI_MAX, 0, MPI_COMM_WORLD);

    unsigned long global_pixels = 0UL;
    MPI_Reduce(&local_pixels, &global_pixels, 1, MPI_UNSIGNED_LONG, MPI_SUM, 0, MPI_COMM_WORLD);

    if (rank == 0)
    {
      const double mae_df =
        global_pixels > 0UL ? global_sum_abs_df / static_cast<double>(global_pixels) : 0.0;
      const double mae_hf =
        global_pixels > 0UL ? global_sum_abs_hf / static_cast<double>(global_pixels) : 0.0;
      std::cout << "=== MPI+混合精度汇总 rank0 ===\n";
      std::cout << "file=" << filename << " sigma=" << sigma << " mpi_size=" << mpi_size << '\n';
      std::cout << "pixels=" << global_pixels << '\n';
      std::cout << "[D vs F] doubleGaussian vs floatGaussian: global_max_abs=" << global_max_abs_df
                << " global_mean_abs=" << mae_df << '\n';
      std::cout << "[D vs H] doubleGaussian vs hybrid_final_float: global_max_abs=" << global_max_abs_hf
                << " global_mean_abs=" << mae_hf << '\n';
    }
  }
  catch (const itk::ExceptionObject & err)
  {
    std::cerr << "rank " << rank << " ITK exception: " << err << '\n';
    MPI_Abort(MPI_COMM_WORLD, 2);
  }

  MPI_Finalize();
  return 0;
}
```

### 6.4 编译命令（第一台）

```bash
cd /home/pub/yyq/mpi_hybrid_precision_demo
rm -rf build && mkdir build && cd build

env -i HOME="$HOME" PATH=/home/pub/gjj/gcc10/bin:/home/pub/cmake/bin:/usr/bin:/usr/local/bin \
  OMPI_CXX=/home/pub/gjj/gcc10/bin/g++ \
  cmake .. \
  -DCMAKE_CXX_COMPILER=/usr/local/bin/mpicxx \
  -DITK_DIR=/home/pub/yyq/ITK-5.4.0/build-yyq

env -i HOME="$HOME" PATH=/home/pub/gjj/gcc10/bin:/home/pub/cmake/bin:/usr/bin:/usr/local/bin \
  cmake --build . -- -j8
```

### 6.5 多机前置：同步二进制到第二台

```bash
ssh root@202.120.87.128 "mkdir -p /home/pub/yyq/mpi_hybrid_precision_demo/build"
scp root@202.120.87.20:/home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo \
    root@202.120.87.128:/home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo
ssh root@202.120.87.128 "chmod +x /home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo"
```

### 6.6 运行命令摘要

**单机多进程（8 ranks，均在本机，便于调试）：**

```bash
mpirun --allow-run-as-root -np 8 \
  /home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo \
  /home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha 2
```

**双机（在第一台发起，`--map-by node`）：**

```bash
mpirun --allow-run-as-root \
  --hostfile /home/pub/yyq_archive/mpi_hosts_2nodes.txt \
  --map-by node -np 8 \
  /home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo \
  /home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha 2
```

**说明：** `root` 运行须 **`--allow-run-as-root`**；生产建议普通用户并去掉该选项。体数据路径在两台必须 **可读且路径一致**（或共享存储）。

---

## 7. 封装脚本 `run_mpi_hybrid_two_nodes.sh`

**本地路径：** [`run_mpi_hybrid_two_nodes.sh`](./run_mpi_hybrid_two_nodes.sh)

```bash
#!/usr/bin/env bash
# 第一台发起：MPI + ImageFileReader 体数据 + 混合精度（默认示例 MHA）。
set -euo pipefail

HOSTFILE="${HOSTFILE:-/home/pub/yyq_archive/mpi_hosts_2nodes.txt}"
NP="${NP:-8}"
EXE="${EXE:-/home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo}"
VOL="${VOL:-/home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha}"
SIGMA="${SIGMA:-2}"

mpirun --allow-run-as-root \
  --hostfile "${HOSTFILE}" \
  --map-by node \
  -np "${NP}" \
  "${EXE}" "${VOL}" "${SIGMA}"
```

从 Windows 上传后执行：**`sed -i 's/\r$//' run_mpi_hybrid_two_nodes.sh`**，再 **`chmod +x`**。

---

## 8. 实测日志归档

以下为 **2026-05-07** 在集群上截取或据此整理的终端输出，用于对照「成功 / 失败」形态。

### 8.1 `itk_hybrid_precision_demo`（单机，成功）

```text
hybrid_precision_demo: float median -> cast double -> Gaussian -> cast float
reference pipeline: float median -> float Gaussian (full float)
[A] double_Gaussian vs float_Gaussian: max_abs=0 mean_abs=0
[B] double_Gaussian vs hybrid_final_float: max_abs=0 mean_abs=0
pixels=65536
```

**说明：** 该滤波器与数据组合下 **float/double 路径数值可完全一致**，差异为 **0** 属可能现象；手册 §2 已解释。

### 8.2 `mpi_hybrid_precision_demo` — 失败日志 A（沿 Z 切分 + 薄层体数据）

曾出现 **`SmoothingRecursiveGaussianImageFilter`** 报错（节选）：

```text
ITK ERROR: SmoothingRecursiveGaussianImageFilter(...): The number of pixels along dimension 2 is less than 4.
This filter requires a minimum of four pixels along the dimension to be processed.
```

**原因：** 示例 **`BrainProtonDensity3Slices.mha`** 层厚维过小；即使读入后 **pad 到 4**，若仍 **沿 Z 切成多 rank**，部分 ROI 在 **Z** 上仍为 **1**。  

**对应修改：** **沿最长空间维切分** + **ROI 二次 pad**（见 §3）。

### 8.3 `mpi_hybrid_precision_demo` — 失败日志 B（第二台二进制未更新）

```text
mpirun was unable to launch the specified application as it could not access or execute an executable:
Executable: /home/pub/yyq/mpi_hybrid_precision_demo/build/mpi_hybrid_precision_demo
Node: 202.120.87.128
```

或第二台运行旧版本仍报 **§8.2** 的 ITK 错误。

**处理：** **`scp`** 同步 **`mpi_hybrid_precision_demo`** 至 **`hpc03`** 同路径并 **`chmod +x`**（§6.5）。

### 8.4 `mpi_hybrid_precision_demo` — 单机 8 进程（成功）

```text
rank 6/8 on hpc01 split_axis=1 range=[162,189) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 3/8 on hpc01 split_axis=1 range=[81,108) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 7/8 on hpc01 split_axis=1 range=[189,217) pixels=20272 local_max|D-F|=0 local_max|D-H|=0
rank 4/8 on hpc01 split_axis=1 range=[108,135) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 1/8 on hpc01 split_axis=1 range=[27,54) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 5/8 on hpc01 split_axis=1 range=[135,162) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 2/8 on hpc01 split_axis=1 range=[54,81) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 0/8 on hpc01 split_axis=1 range=[0,27) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
=== MPI+混合精度汇总 rank0 ===
file=/home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha sigma=2 mpi_size=8
pixels=157108
[D vs F] doubleGaussian vs floatGaussian: global_max_abs=0 global_mean_abs=0
[D vs H] doubleGaussian vs hybrid_final_float: global_max_abs=0 global_mean_abs=0
```

### 8.5 `mpi_hybrid_precision_demo` — 双机 8 进程（成功）

```text
rank 6/8 on hpc01 split_axis=1 range=[162,189) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 0/8 on hpc01 split_axis=1 range=[0,27) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 4/8 on hpc01 split_axis=1 range=[108,135) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 2/8 on hpc01 split_axis=1 range=[54,81) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 3/8 on hpc03 split_axis=1 range=[81,108) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 7/8 on hpc03 split_axis=1 range=[189,217) pixels=20272 local_max|D-F|=0 local_max|D-H|=0
rank 5/8 on hpc03 split_axis=1 range=[135,162) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
rank 1/8 on hpc03 split_axis=1 range=[27,54) pixels=19548 local_max|D-F|=0 local_max|D-H|=0
=== MPI+混合精度汇总 rank0 ===
file=/home/pub/yyq/ITK-5.4.0/Examples/Data/BrainProtonDensity3Slices.mha sigma=2 mpi_size=8
pixels=157108
[D vs F] doubleGaussian vs floatGaussian: global_max_abs=0 global_mean_abs=0
[D vs H] doubleGaussian vs hybrid_final_float: global_max_abs=0 global_mean_abs=0
```

可见 rank 分布在 **`hpc01` / `hpc03`**；**`split_axis=1`** 表示当前数据沿 **索引维 1**（实现中选定的「最长维」）切块。

---

## 9. 相关索引

| 文档 / 目录 | 用途 |
|-------------|------|
| [`操作记录_ITK备份与混合精度_2026-05-07.md`](./操作记录_ITK备份与混合精度_2026-05-07.md) | 备份与混合精度简明版 |
| [`操作记录_MPI双机环境与运行_2026-05-07.md`](./操作记录_MPI双机环境与运行_2026-05-07.md) | MPI hostfile、脚本、`mpi_itk_smoke`、新手第 0 节 |
| [`操作记录_ITK编译与单节点并行绑核_2026-05-07.md`](./操作记录_ITK编译与单节点并行绑核_2026-05-07.md) | **`build-yyq`**、GCC/CMake、`env -i` |
| [`itk_hybrid_precision_demo/`](./itk_hybrid_precision_demo/) | 单机源码 |
| [`mpi_hybrid_precision_demo/`](./mpi_hybrid_precision_demo/) | MPI+Reader 源码 |

---

*本手册为 2026-05-07 混合精度相关修改、代码与日志的完整归档；以后代码若以仓库文件为准有差异，以 Git/文件内容为准并相应更新本节。*
